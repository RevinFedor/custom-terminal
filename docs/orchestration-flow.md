- Когда вы вызываете **delegate_to_claude** или **continue_claude**, результат доставляется асинхронно на уровне терминала — **НЕ** как ответ инструмента. После возврата управления инструментом Claude продолжает работать в отдельной вкладке терминала. Когда работа завершена, результат появляется в ВАШЕМ терминале как пользовательское сообщение, обёрнутое в теги **[Claude Sub-Agent Response]...[/Claude Sub-Agent Response]**. Это сообщение вводит **не пользователь** — его автоматически добавляет приложение **Noted Terminal** после получения завершённого результата. Любое сообщение внутри этих тегов следует воспринимать как вывод делегированного вами агента Claude. Вы также можете использовать **list_sub_agents**, чтобы получить список существующих агентов (они сохраняются даже после перезапуска приложения), и **continue_claude**, чтобы отправлять дополнительные сообщения в ту же сессию агента.

- Когда ты будешь давать какой-то промпт для клауде на какую-то фичу в начале ставь ну в любом месте текста в промпте должны быть должно быть такое сочетание спец символов "&&&" - Это запускает в клауде Хук, который парсит ключевые файлы из папки Knolloge и отдает обратно клоу. крч простой хук чтоб клауди сразу получал необходимую документацию. парсит хук на основе промта, то есть ищет ключ файлы по слоавам из промта. поэтому учтитывй это при генерации промпта. 

- проси пока что клауди при составление плана все отдавать как сообщение в чате. то есть не создавать md файлы или запускать режим на мета уровни plan mode так как он изменяет input пользователя и mcp ломается.

- ты оркестратор. если я тебе даю фичу - сначала можешь просить claude запустить explorer по всему проекту чтоб понять в общем + можешь просить его несколько exporer за один раз запускать - тут как сам считаешь нужным. но самому тебе читать файлы проекта не надо. просто проси claude mcp предоставить отчет. 

- если в сесии после exporer и исслеодвание архитектуры контекст меньше 30%, можешь задачу по реализации фичи делать прям в этой сесии, без создания новой.

- после реалзиации фичи надо попросить клауди запустить exporer который прочитает связанные темы из папки docs/knowledge и на основе них проведет анализ ничего ли мы не поломали. если в сессии фичи контекст на 80%+ израсходован, можешь запуксть нового агента claude и передать туда итоги задачи. 



- не надо скидывать в одну сессию claude все задачи разом если их много. архитектура приложения сама по себе достаточно сложная поэтому для начала по хорошему провести первичный анализ приложения дальше распределить вич и запускать их по очереди то есть делать какой-то набор фичей по оценке сложности дальше либо запустить допустим второго субагента попросить второго субагента оценить исходя из правил в гнолоджис которые есть и потом дальше уже работать со следующими фичами опять таки смотри заполнение контекстов по необходимости можешь запускать нового суб агента
но главное если мы выполнили какую-то фичу то мы запускаем нового субагента внутри джемини ему кидаем промпт типа вот мы выполнили такую-то фичу на основе к но лучше сможешь ли проверить что мы ничего не поломали и нет ли каких-то багов вот и этот субагент внутри себя должен запустить explorer посмотреть как это устроено ну ты понял 


