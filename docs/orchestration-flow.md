оснеовная часть в gemini.md

о есть условно я прям где в клауде 
   настрою там Flow что типа запускай после разработки оркестратор тесты вот. То есть условно клауде, ещё раз блять как работает        
  Flow вот это очень важно. Оргистратор запускает первого агента, первого агента делает полноценный ресерч, отдает оркестратору       
  агента, запускает второй агент он делает Feature потом третий агент запускает Play-Write тесты возможно в ходе тестирования будут     
   проблемы поэтому третий агент переписывает тесты до тех пор пока у него не получится вот возможно возникнут какие-то               
  фундаментальные проблемы поэтому опять таки он отдаст если что обратно в оркестратора результат выполнения работы оркестратора    
  может запустить четвертого агента для более подробного ресёрча по какой-то конкретной технологии тестирования потом обратно           
  отдать третьему агенту опять-таки все это также будет настраиваться в прамптах то есть все эти правила взаимодействия они             
  настраиваются через прампты и дальше второй агент завершает Play-Write тесты и все отлично вот возможно кстати еще тоже другой        
  флоу во время реализации фичи могут быть какие-то проблемы и ну типа опять таки будет прописано в прампте что если у агента           
  проблемы он должен обращать обращаться к оркестратору, он может вернуться к первой сессии и дополнительно какие-то дать задачи по     
   ресёрчу вот но в итоге после всего того что сделано оркестратор также на основе всех функциональностей первому агенту кидает         
  список того что мы сделали и просит на основе какого-то прампта обновить документацию то есть такое кросс-агентное взаимодействие     
   опять таки флоу как это все работает будет прописано в главном файле