# Feature: Claude History Timeline

## Intro
Таймлайн — это вертикальная панель справа от терминала, отображающая хронологию сообщений в текущей сессии Claude Code. Позволяет быстро визуализировать прогресс и перемещаться к конкретным точкам диалога.

## User Flow
1. Пользователь запускает `claude` в терминале.
2. Справа появляется полоса с точками. Каждая точка — сообщение пользователя.
3. При наведении на точку появляется всплывающее окно с превью текста промпта.
4. При клике на точку терминал автоматически скроллится к месту, где этот промпт был введен.

## Behavior Specs
- **Event-Driven:** Таймлайн появляется только при активном процессе Claude (отслеживается через Shell Integration OSC 133).
- **Smart Filtering:**
    - Скрываются системные сообщения и результаты инструментов (`tool_result`).
    - Игнорируются отмененные через Escape/Undo ветки диалога (алгоритм Backtrace). См. `knowledge/fix-jsonl-backtrace.md`.
    - Очищаются технические последовательности (Bracketed Paste).
- **Navigation:** Используется `SearchAddon` для поиска текста промпта в буфере xterm.js. Если текст не найден (буфер очищен), переход не происходит.

## Code Map
- `Timeline.tsx`: UI компонент (полоса, точки, портал для тултипов).
- `main.js` (`claude:get-timeline`): Парсинг и фильтрация JSONL.
- `terminalRegistry.ts`: Метод `searchAndScroll` для управления xterm.js.
