# AI Backtrace JSONL: Reverse Traversal, Compact Recovery & Bridge Following

### Симптомы
- **Дублирование:** В Timeline (ленте истории) отображаются сообщения, которые пользователь отменил кнопкой Undo/Escape, перемешиваясь с новыми ответами.
- **Обрыв истории:** Лента внезапно заканчивается на середине длинного диалога (обычно после выполнения команды `/compact`), и старые сообщения становятся недоступны для просмотра.
- **Подмена сессии после форка:** После закрытия вкладки с форком и возврата на оригинальную вкладку, session ID перезаписывается на ID форка. `/status` в терминале показывает один ID, а приложение — другой. Причина: `resolveLatestSessionInChain` не смог отличить форк-копию от bridge (Clear Context), и chain resolver «склеил» независимые ветки.

---

## 1. Навигация по JSONL через Backtrace

### Проблема: "Мёртвые" ветки диалога
Файлы `.jsonl`, которые генерирует Claude Code, являются журналом событий (append-only log), а не готовым списком сообщений.
Когда пользователь нажимает **Escape (Undo)**:
1. Записи в файле НЕ удаляются.
2. В UI Claude откатывается к предыдущему состоянию.
3. Новое сообщение создаёт "вилку" (новую ветку) от старого родителя.

Если читать файл просто сверху вниз (как массив), Timeline покажет и отменённые сообщения, и новые, что ломает логику.

### Решение: Алгоритм Обратной Трассировки (Backtrace)
Вместо чтения подряд, мы собираем историю "с конца":

1. **Индексация (Incremental Reader):** Читаем цепочку файлов (через `resolveSessionChain`). Для оптимизации используется асинхронный инкрементальный ридер (см. ниже). Каждой записи при загрузке присваиваются поля `_fileIndex` и `_fromFile`.
2. **Фильтрация шума:** При парсинге JSONL система автоматически вырезает системный мусор, такой как `<task-notification>`.
3. **Определение головы:** Берем самую последнюю запись в последнем файле цепи.
4. **Обход:** Идем от "головы" назад к корню, используя поле `parentUuid`.
5. **Компакция:** Если встречаем маркер компакции, используем логику **Compact Gap Recovery**.
6. **Bridge Following:** Если `parentUuid` отсутствует, ищем запись-мост (`_isBridge`). Дополнительно проверяется отсутствие данной сессии в `fork_markers` (см. раздел 3).
7. **Результат:** Инвертируем собранный список.

---

## 1.1. Incremental Reader & Performance

### Проблема синхронного чтения
При размере файла >100MB синхронное чтение (`fs.readFileSync`) и полный парсинг JSON заново каждые 2 секунды блокируют Main-процесс на 3+ секунды, что вызывает фатальные фризы UI.

### Решение: Чтение только новых байтов
Для файлов сессий реализован механизм инкрементальной загрузки:
1. **mtime/size Cache:** Система хранит кеш парсированных записей, привязанный к `mtime` и размеру файла.
2. **Byte Offset:** Если файл вырос, ридер открывает его и переходит сразу к последнему известному смещению (`lastOffset`).
3. **Leftover Buffer:** Если последний чанк закончился на середине строки (разрыв JSON), остаток сохраняется в `leftover` и склеивается с началом следующего чтения.
4. **Async Parsing:** Чтение выполняется асинхронно через `fs.promises`, предотвращая блокировку Event Loop.

---

## 2. The Compact Gap Recovery (Dangling UUIDs)

### Природа Compact в Claude Code
Критически важно понимать: команда `/compact` **НЕ удаляет** записи из JSONL-файла. Это append-only лог. Все pre-compact записи физически сохранены на диске.
Claude только добавляет запись типа `compact_boundary`, где поле `logicalParentUuid` указывает на UUID последнего сообщения перед сжатием.

**Проблема:** Этот UUID часто является "dangling reference" — он существует в оперативной памяти Claude, но никогда не записывался в файл. Стандартный Backtrace, наткнувшись на такой UUID, останавливается, "теряя" всю историю до компакта.

### Двухуровневое восстановление (Recovery Logic)
Если `recordMap.get(uuid)` возвращает `null` сразу после `compact_boundary`, алгоритм применяет два уровня спасения цепочки:

1. **Level 1: parentUuid Fallback.** Проверяется наличие обычного `parentUuid` у записи компакта. Если он указывает на валидную запись в Map, трассировка продолжается по нему.
2. **Level 2: Physical Predecessor.** Если Level 1 не помог, алгоритм переходит к **физическому поиску**: в том же файле (`_fromFile`) ищется запись с максимальным `_fileIndex`, который строго меньше индекса текущего компакта. Поскольку JSONL линеен, эта запись гарантированно является хронологическим предшественником.

---

## 2.1. Compact Summary Integration
Сжатие теперь является не только техническим разрывом, но и полноценным интерактивным элементом в ленте истории. Система использует механизм "заглядывания вперед" (look-ahead), чтобы найти текст саммари в последующих сообщениях и отобразить его внутри раскрывающегося блока `COMPACTED`. См. [`fix-compact-logic.md`](fix-compact-logic.md).

---

## 3. Bridge Following: Защита от циклов

### Проблема
При наличии в `mergedMap` записей из 3 и более файлов сессий, в общем индексе может оказаться несколько записей-мостов (`_isBridge`). Обычный поиск моста по `sessionId` может привести к выбору моста, который ведет назад к уже посещенным записям, вызывая бесконечный цикл или преждевременный обрыв.

### Решение
При поиске моста в алгоритме Backtrace введена проверка: `!seen.has(entry.parentUuid)`.
Мы следуем только по тем мостам, чья родительская запись еще не была обработана. Это гарантирует строго направленное движение к корню цепочки (root session).

### Invisible Intent: Internal ID Static (Fork vs Bridge)
Наш механизм форка (`copyFileSync`) просто копирует JSONL-файл целиком. В отличие от нативного форка Claude CLI, который перезаписывает `sessionId` внутри записей, в наших копиях **все старые записи сохраняют оригинальный ID**.
- **Следствие:** Любой файл-форк выглядит для алгоритма как «Bridge-ребенок» родительской сессии, так как его первая запись ссылается на старый ID.
- **Решение:** В логику `resolveLatestSessionInChain` добавлена проверка через `getParentSession(fId)`. Если файл обнаружен в базе данных форков (`fork_markers`), он **пропускается** как потенциальный мост Plan Mode. Это предотвращает ложное «склеивание» веток диалога, которые были намеренно разделены пользователем через Fork.

---

## 3.1. Fork Copies Bridges (Ловушка при форке)

### Проблема
При форке сессии приложение **копирует JSONL-файл** целиком. Внутри копии сохраняются все bridge-записи (`_isBridge`) от оригинальной цепи. Когда `resolveSessionChain` обрабатывает форкнутый файл, он следует по этим скопированным мостам и попадает в оригинальную цепь, **пропуская промежуточные сессии**.

### Пример
Оригинальная цепь: `A → B(plan mode) → C(plan mode)`.
Форк из `C` → создаёт `D.jsonl` (копия `C.jsonl`).
В `D.jsonl` есть bridge к `B` (из оригинала).
`resolveSessionChain(D)` строит цепь: `D → B → A`. Сессия `C` пропущена.

### Следствие для UI
`sessionBoundaries` из `resolveSessionChain` НЕ содержит всех plan mode переходов. Записи внутри `entries` при этом СОДЕРЖАТ правильные `sessionId` (из оригинальных данных).

## 3.2. Sub-agents (Task tool) Inline Storage

Claude Code записывает работу субагентов (вызываемых через инструмент `Task`) **инлайн** в основной JSONL-файл родительской сессии.

### Особенности хранения
- **Тип записей:** `type: "progress"`, `data.type: "agent_progress"`.
- **Связь:** Эти записи НЕ содержат `parentUuid` для включения в основную цепочку Backtrace. Они связаны с родительским сообщением через поле `parentToolUseID` (которое соответствует `uuid` записи `tool_use` ассистента).
- **Финальный результат:** Текстовый ответ субагента хранится в записи `type: "user"` с полем `tool_result`, соответствующим ID вызова `Task`.

### Логика сбора
При парсинге JSONL система собирает все `agent_progress` записи в отдельный массив `progressEntries`. При отображении или экспорте сессии эти записи группируются по их `parentToolUseID`, позволяя восстановить «вложенную» историю диалога субагента (его мысли, действия и ответы) внутри родительского контекста.

### Trap: Broken Chains after Manual Intervention
Ручной ввод пользователем (после нажатия Escape в табе субагента) может привести к тому, что `readLatestAssistantMessage` не сможет найти текст ответа (`null`).
- **Факт:** Цепочка `parentUuid` в JSONL может стать нелинейной или "оборванной" для алгоритма обратного обхода.
- **Диагностика:** В систему внедрено расширенное логирование (шаги обхода, типы блоков, точки разрыва) для отладки таких случаев.

