Сессия уже обработана внешним агентом. Ниже готовые инструкции по изменению файлов — не нужно заново читать или анализировать сессию, только примени указанные правки.

Отлично! Все тесты зелёные (кроме известного fixture drift в rewind). Рефакторинг завершён успешно.

---

## Финальный отчёт

### Что сделано

**1. Рефакторинг ActionsPanel.tsx**
- Извлечены 2 shared-хелпера: `exportSessionContent()`, `getDocumentationPrompt()`
- Убрано ~60 строк дублированного кода из 3 функций

**2. Модуляризация main.js**

| Модуль | Строк | IPC хэндлеры | Зависимости |
|--------|-------|--------------|-------------|
| `ipc/docs.js` | 80 | 3 (docs:*) | Нет |
| `ipc/settings.js` | 110 | 10 (research:*, commands:*, prompts:*, ai-prompts:*) | projectManager |
| `ipc/claude-data.js` | 1617 | 6 + 5 helpers | projectManager, formatToolAction |
| `ipc/gemini-data.js` | 485 | 9 | projectManager, geminiUtils, terminals, terminalProjects, geminiHistoryWatchers |

**main.js:** 5790 → 3637 строк (-37%)

**3. E2E test infrastructure**
- `NOTED_E2E_TEST` env flag — подавляет нативные error dialogs
- `auto/stable/test-ipc-smoke.js` — 17 проверок по всем модулям

**4. Тестирование**
- 126 тестов прогнано, 125 passed
- Единственный fail — pre-existing fixture drift (не связан с рефакторингом)

### Что осталось в main.js

**Ядро приложения** (3637 строк):
- PTY lifecycle (`terminal:create`, onData pipeline)
- Claude handshake, agent detection, Ctrl-C danger zone
- Gemini spawn-with-watcher, Sniper
- Boundary markers (OSC 133, OSC 7777)
- TUI navigation (`claude:open-history-menu`, `gemini:open-history-menu`)
- App lifecycle, window, menu

Это сильно переплетённый код с зависимостями на 15+ shared Map'ов — разделять его дальше нецелесообразно.

### Преимущества для AI-агента

**До:**
- Read main.js (5790 строк) = ~20K токенов контекста
- Edit в монолите — высокий риск промаха

**После:**
- Read focused module (80-1600 строк) = 1-5K токенов
- Edit точнее в маленьких файлах
- Grep работает одинаково хорошо

---